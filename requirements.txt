# Local scripts (data-loader, kafka consumer, etc.)
# Not used inside Docker — each service manages its own dependencies.
pandas>=2.0.0
psycopg2-binary>=2.9.0
confluent-kafka>=2.3.0
fastavro>=1.9.0
boto3>=1.28.0
python-dotenv>=1.0.0
requests>=2.31.0
